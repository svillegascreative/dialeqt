<h1>Add a Word</h1>

<%= form_tag check_words_path, method: :get do %>
  <div class="form-field">
    <%= text_field_tag(
    :name,
    params[:name]
    ) %>
  </div>
  
  <div class="form-actions">
    <%= submit_tag "Submit word", name: nil %>
  </div>
<% end %>
