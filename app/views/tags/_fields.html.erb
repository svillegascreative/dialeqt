<fieldset class="tag-fields">
  <legend>Tags</legend>
  <%= f.fields_for :tags do |tags_f| %>
    <div class="form_field">
      <%= tags_f.text_field :name %>
      <%= tags_f.check_box :_destroy %>
      <%= tags_f.label :_destroy, "Remove tag" %>
    </div>
  <% end %>
</fieldset>
