<%= form_tag search_path, method: :get, class: "search-form" do %>
  <%= text_field_tag(
      :query,
      nil,
      placeholder: "What does it mean?"
  ) %>
  <%= submit_tag "Go!", {name: nil, class: "button-on-right"}  %>
<% end %>
